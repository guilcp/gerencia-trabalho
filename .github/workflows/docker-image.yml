name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
  
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: start docker
        run: docker-compose up -d    
      - name: testa front 
        run: docker exec frontend sh -c "CI=false npm run test"
      - name: testa back
        run: docker exec backend sh -c "pyhton3 manane.py test"
